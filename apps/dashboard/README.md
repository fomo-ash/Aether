# apps/dashboard
**Purpose:** Visualizes workflows. Displays DAG, worker status, logs, queue status, and retries.
**What it should NEVER do:** Do not execute workflows or plan them.
**Future scalability notes:** Standalone frontend app.
**Dependencies:** @flowpilot/api-client (future)
